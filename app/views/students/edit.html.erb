<%= content_tag :pre, @student.errors.to_yaml if @student.errors.presence %><br/>

<%= form_for @student, :url => {:action => :update} do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <br/><%= f.submit "Student speichern" %>
<% end %>