<%= render :partial => 'header' %>

<%= content_tag :pre, @course.errors.to_yaml if @course.errors.presence %><br/>

<% form_for @course, :action => :create do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <br/><%= f.submit "Kurs anlegen" %>
<% end %>